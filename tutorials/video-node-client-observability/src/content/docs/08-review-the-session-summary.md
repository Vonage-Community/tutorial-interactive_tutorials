---
title: Review the Session Summary
description: Confirm that healthy telemetry produces useful session totals.
---

# Review the Session Summary

Return to the monitor. Select **Reset data**, then run **Healthy baseline** again.

The quality summary should now show:

- `1` publisher sample,
- `1` subscriber sample,
- `0` warning samples,
- `0` affected streams.

The summary answers a session-level question while the table preserves the individual records needed for investigation.

The next function will keep a single warning from becoming an operational alert.
