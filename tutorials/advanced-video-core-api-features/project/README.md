# Advanced Video Core API Features Project

This project runs the app and validation API for the Advanced Video Core API Features learning path.

Run the app server:

```sh
npm start
```

Configure Vonage credentials:

```sh
npm run setup-credentials
```
