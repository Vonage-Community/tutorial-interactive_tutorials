{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Auto Open Live Preview",
            "type": "shell",
            // The '#' trick safely fires the UI command internally without throwing a shell path execution error
            "command": "echo 'Initializing secure viewport layout...' # ${input:launchLivePreview}",
            "runOptions": {
                "runOn": "folderOpen"
            },
            "presentation": {
                "reveal": "silent",
                "panel": "shared"
            }
        }
    ],
    "inputs": [
        {
            "id": "launchLivePreview",
            "type": "command",
            "command": "livePreview.start"
        }
    ]
}
