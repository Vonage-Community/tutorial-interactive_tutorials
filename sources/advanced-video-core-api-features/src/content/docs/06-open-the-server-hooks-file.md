---
title: "Open the server hooks file"
description: "Debug timeline"
---

Debugging Basics uses both browser events and server callbacks to build the timeline.

In the Codespace editor, open:

```text
src/advanced-video-server-hooks.js
```

Find `recordClientDebugEvent` and `recordVideoCallback`.
